import React from 'react';

const Blogs = () => {
  return (
    <div className='text-center'>
      <h1 className='text-3xl'>What is the perpase of useContext API</h1>
      <p>Context APIs enable us to define the context Object which stores some data and will make it available throughout the hierarchy without passing the data as props</p>
      <h1 className='text-3xl'>What is symantic tag</h1>
      <p>Semantic HTML elements are those that clearly describe their meaning in a human- and machine-readable way. Elements such as header , footer and article are all considered semantic because they accurately describe the purpose of the element and the type of content that is inside them</p>
    </div>
  );
};

export default Blogs;